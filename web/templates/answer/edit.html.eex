<h2>Edit answer</h2>

<%= render "form.html", changeset: @changeset,
                        action: answer_path(@conn, :update, @answer) %>

<span><%= link "Back", to: answer_path(@conn, :index) %></span>
