<h2>Edit session</h2>

<%= render "form.html", changeset: @changeset,
                        action: session_path(@conn, :update, @session) %>

<span><%= link "Back", to: session_path(@conn, :index) %></span>
